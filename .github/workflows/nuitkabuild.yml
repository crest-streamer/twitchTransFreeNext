name: Build Binaries and Create Release

on:
  push:
    tags:
      - "cv*"

env:
  GPG_KEY_ID: ${{ secrets.GPG_KEY_ID }}
  GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
  GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
  GPG_PUBLIC_KEY: ${{ secrets.GPG_PUBLIC_KEY }}

jobs:
  build:
    strategy:
      matrix:
        include:
          - env: linux_appimage
            os: ubuntu-latest
            bin: twitchTransFN
            archive: AppImage
            platform: native
          - env: windows
            os: windows-latest
            bin: twitchTransFN.exe
            archive: zip
            platform: native
          - env: macos_M1
            os: macos-latest
            bin: twitchTransFN.app
            archive: tar.gz
            arch: arm64
            platform: native
          - env: macos_Intel
            os: macos-latest
            bin: twitchTransFN.app
            archive: tar.gz
            arch: x86_64
            platform: native

    runs-on: ${{ matrix.os }}
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install system dependencies (Linux only)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y patchelf zstd curl file libfuse2

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install nuitka
          pip install -r requirements.txt
        shell: bash

      - name: Convert .ico to .icns on macOS
        if: matrix.os == 'macos-latest'
        run: |
          mkdir icon.iconset
          sips -z 16 16     icon.png --out icon.iconset/icon_16x16.png
          sips -z 32 32     icon.png --out icon.iconset/icon_16x16@2x.png
          sips -z 32 32     icon.png --out icon.iconset/icon_32x32.png
          sips -z 64 64     icon.png --out icon.iconset/icon_32x32@2x.png
          sips -z 128 128   icon.png --out icon.iconset/icon_128x128.png
          sips -z 256 256   icon.png --out icon.iconset/icon_128x128@2x.png
          sips -z 256 256   icon.png --out icon.iconset/icon_256x256.png
          sips -z 512 512   icon.png --out icon.iconset/icon_256x256@2x.png
          sips -z 512 512   icon.png --out icon.iconset/icon_512x512.png
          cp icon.png           icon.iconset/icon_512x512@2x.png
          iconutil -c icns icon.iconset -o icon.icns

      - name: Download cacert.pem
        run: curl -o cacert.pem https://curl.se/ca/cacert.pem

      - name: Create dist directory
        run: mkdir -p dist

      - name: Build with Nuitka
        shell: bash
        run: |
          ICON_OPT=""
          if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            ICON_OPT="--windows-icon-from-ico=icon.ico"
          elif [[ "${{ matrix.os }}" == "macos-latest" ]]; then
            ICON_OPT="--macos-app-icon=icon.icns --macos-create-app-bundle"
          elif [[ "${{ matrix.os }}" == "ubuntu-latest" ]]; then
            ICON_OPT="--linux-icon=icon.ico"
          fi

          BUILD_CMD="python -m nuitka --standalone --onefile --output-dir=dist \
            --follow-imports --remove-output --assume-yes-for-downloads \
            $ICON_OPT twitchTransFN.py"

          if [[ "${{ matrix.os }}" == "macos-latest" ]]; then
            arch -${{ matrix.arch }} bash -c \"$BUILD_CMD\"
          else
            eval "$BUILD_CMD"
          fi

      - name: Create AppImage (Ubuntu only)
        if: matrix.env == 'linux_appimage'
        run: |
          wget -q https://github.com/AppImage/AppImageKit/releases/download/13/appimagetool-x86_64.AppImage
          chmod +x appimagetool-x86_64.AppImage
          mkdir -p AppDir/usr/bin
          cp dist/${{ matrix.bin }} AppDir/usr/bin/
          cp icon.png AppDir/
          echo '[Desktop Entry]
          Name=twitchTransFN
          Exec=twitchTransFN
          Icon=icon
          Type=Application
          Categories=Utility;' > AppDir/twitchTransFN.desktop
          ./appimagetool-x86_64.AppImage AppDir twitchTransFN_${{ steps.get_version.outputs.version }}_${{ matrix.env }}.AppImage

      - name: Get Version
        id: get_version
        run: |
          VERSION=$(grep -oP "version\s*=\s*['\"]\K[0-9\.]+(?=['\"])" twitchTransFN.py)
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Prepare archive folder
        if: matrix.env != 'linux_appimage'
        run: |
          ARCHIVE_NAME="twitchTransFN_${{ steps.get_version.outputs.version }}_${{ matrix.env }}"
          mkdir -p "$ARCHIVE_NAME"
          cp -R dist/${{ matrix.bin }} "$ARCHIVE_NAME/"

      - name: Import GPG Keys
        if: ${{ env.GPG_PRIVATE_KEY != '' }}
        run: |
          echo "$GPG_PRIVATE_KEY" | gpg --batch --import
          echo "$GPG_PUBLIC_KEY" | gpg --batch --import

      - name: GPG Sign Binary
        if: ${{ env.GPG_PRIVATE_KEY != '' && env.GPG_KEY_ID != '' && matrix.env != 'linux_appimage' }}
        run: |
          ARCHIVE_NAME="twitchTransFN_${{ steps.get_version.outputs.version }}_${{ matrix.env }}"
          BIN_PATH="$ARCHIVE_NAME/${{ matrix.bin }}"
          echo "$GPG_PASSPHRASE" | \
            gpg --batch --yes --pinentry-mode loopback --passphrase-fd 0 \
              --local-user "$GPG_KEY_ID" \
              --output "$BIN_PATH.sig" \
              --detach-sign "$BIN_PATH"
          mv "$BIN_PATH.sig" "$ARCHIVE_NAME/"

      - name: Final archive with .sig
        if: matrix.env != 'linux_appimage'
        run: |
          ARCHIVE_NAME="twitchTransFN_${{ steps.get_version.outputs.version }}_${{ matrix.env }}"
          if [[ "${{ matrix.archive }}" == "zip" ]]; then
            zip -r "${ARCHIVE_NAME}.zip" "$ARCHIVE_NAME"
          else
            tar -czvf "${ARCHIVE_NAME}.tar.gz" "$ARCHIVE_NAME"
          fi

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            twitchTransFN_${{ steps.get_version.outputs.version }}_${{ matrix.env }}.${{ matrix.archive }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Cleanup
        run: rm -rf twitchTransFN_*
